repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.27.2
    hooks:
      - id: gitleaks
        name: Detect secrets (gitleaks)
        description: Detect hardcoded secrets, API keys, tokens in staged files
