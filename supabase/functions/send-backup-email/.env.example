# Resend API key — add via Supabase Dashboard → Settings → Edge Functions → Secrets
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Sender email address — must be a verified domain in Resend
# For testing, use: onboarding@resend.dev (Resend's shared test address)
RESEND_FROM_EMAIL=noreply@zafeer.app

# The following are auto-injected by Supabase Edge Function runtime — DO NOT set manually:
# SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY
